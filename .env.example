# Server only. Never prefix with VITE_ — that would ship to the browser.
# Google / handset number for SMS via the paired phone. Leave blank to notify only.
RNK_SMS_TO=
KDECONNECT_DEVICE_ID=

# Production server (server/prod.ts). Defaults to 4000 if unset.
PORT=4000
